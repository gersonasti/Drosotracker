# Artefactos locales de pruebas (no forman parte de la app)
.playwright-mcp/
*.png

# Figuras generadas por analysis/calibracion.py (salida determinística; se regeneran)
analysis/figures/

# Planillas generadas por analysis/exportar_tablas.py (salida determinística; se regeneran)
analysis/tablas/
# ...excepto los íconos del PWA (sí forman parte de la app)
!icon-192.png
!icon-512.png
!apple-touch-icon.png

# Backups y exportaciones de datos del usuario (no van al repo)
drosotracker-backup*.json
drosotracker-*.csv

# Documentos de trabajo internos (no se despliegan al sitio público)
DrosoTracker-funcionalidades.md
DrosoTracker-*.md
