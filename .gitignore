# Artefactos locales de pruebas (no forman parte de la app)
.playwright-mcp/
*.png
# ...excepto los íconos del PWA (sí forman parte de la app)
!icon-192.png
!icon-512.png
!apple-touch-icon.png

# Backups y exportaciones de datos del usuario (no van al repo)
drosotracker-backup*.json
drosotracker-*.csv

# Documento de trabajo interno (no se despliega al sitio público)
DrosoTracker-funcionalidades.md
